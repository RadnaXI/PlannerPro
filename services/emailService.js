const sendEmail = (recipient, subject, body) => {
    // Implementace odeslání e-mailu
    console.log(`Odesílání e-mailu na adresu ${recipient} s předmětem: ${subject}`);
  };
  
  module.exports = {
    sendEmail
  };